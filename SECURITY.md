# 安全策略

## 报告安全问题

请不要通过公开Issue披露可利用漏洞。请使用GitHub仓库的“Security → Advisories → Report a vulnerability”私密报告入口。

报告建议包含：

- 影响范围和受影响版本或提交
- 最小复现步骤
- 预期与实际安全边界
- 建议修复或缓解措施

不要在报告中附带真实授权码、Cookie、管理员密码、私钥、完整生产日志或用户数据。

## 支持范围

安全修复以GitHub默认分支最新代码为准。当前项目仍处于 `0.x` 阶段，尚未维护多个并行支持版本。

## 仓库敏感数据

以下内容不得提交：

- 生产服务器IP、域名和拓扑细节
- `/etc/private-video.env`
- `/root/private-video-admin.txt`
- MySQL、Redis和管理员密码
- 真实授权码、播放令牌、聊天访客令牌和Cookie
- MySQL转储、备份包、私钥和TLS私钥
- 原视频、HLS文件
- 未脱敏日志、IP、观看记录、聊天记录和截图

提交前应执行敏感信息扫描，并检查新增的测试夹具、日志和文档示例。

## 应用安全设计

- 授权码只保存HMAC摘要和尾号，明文仅在创建结果页显示一次。
- 播放令牌和聊天访客令牌只保存摘要，浏览器Cookie使用HttpOnly。
- 管理写操作要求staff登录、POST、CSRF、输入校验和管理员审计。
- 原视频与HLS目录不公开；媒体分片需要有效会话和短期签名。
- 上传先写临时文件，验证格式、容器、视频轨道、大小和目标路径后原子移动。
- WebSocket校验来源、访客令牌、聊天室状态、IP一致性和IP黑名单。
- 聊天消息先提交MySQL，再通过Redis广播。
- 授权、聊天发送和黑名单事件均有限流或审计。
- 关闭共享、删除或轮换授权码、关闭聊天室以及主动退出都会撤销相关会话。

## 生产安全基线

- 使用MySQL 8和独立最小权限应用用户。
- Redis只允许受信网络访问并设置强密码。
- Gunicorn `127.0.0.1:8008` 与Daphne `127.0.0.1:8009` 不对公网开放。
- 只通过Nginx对外提供服务。
- 使用可信HTTPS证书，并启用 `COOKIE_SECURE=1` 和 `SECURE_SSL_REDIRECT=1`。
- 正确设置 `CSRF_TRUSTED_ORIGINS`。
- 只有请求无法绕过可信代理时才启用 `TRUST_PROXY_HEADERS=1`。
- 限制环境文件、管理员凭据、数据库备份、原视频和HLS目录的文件权限。
- 定期升级Python依赖、系统包、Nginx、MySQL、Redis和FFmpeg。
- 部署后运行 `manage.py check --deploy`、冒烟测试并检查warning/error日志。

## 已知边界

- 浏览器必须接收媒体数据才能播放，因此无法保证视频绝对不能被提取、缓存、截屏或录屏。
- IP地址可能受NAT、代理或共享网络影响，不能等同于自然人身份。
- 小说人物昵称只是聊天室内显示标识，不构成用户实名或持久身份。
- 黑名单和应用层限流用于降低滥用风险，不能替代防火墙、WAF或上游DDoS防护。
